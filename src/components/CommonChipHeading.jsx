import React from 'react'

const CommonChipHeading = ({chipTitle}) => {
  return (
   <div className=" font-semibold mb-8">
            <h2 className='text-gray-300 text-lg md:text-xl uppercase font-bold'>{chipTitle}</h2>
          </div>
  )
}

export default CommonChipHeading